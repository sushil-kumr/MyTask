package com.sushildlh.mytasks.Modal

class TaskResponse{
    var pizzaData:List<MenuData> = emptyList()
    var drinkData:List<MenuData> = emptyList()
    var sushiData:List<MenuData> = emptyList()
    var discountImages:List<SliderItem> = emptyList()
}